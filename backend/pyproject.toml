[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vampter-backend"
version = "1.0.0"
description = "AI Policy Document Auditor Backend"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]