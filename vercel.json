{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "rootDir": "frontend"
      }
    },
    {
      "src": "backend/main.py",
      "use": "@vercel/python",
      "config": {
        "rootDir": "backend",
        "includeFiles": [
          "backend/**/*.py",
          "backend/**/*.toml",
          "backend/requirements.txt"
        ]
      }
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/api/(.*)",
      "dest": "/backend/main.py"
    }
  ],
  "functions": {
    "backend/main.py": {
      "maxDuration": 300
    }
  }
}